# Data

This folder includes the data set used for the analysis, which was generated with the `eda.R` file in the `../src/plots` directory.

### Directory roadmap
* `tour-de-france-data.csv` is the data set
